# This workflow will build a golang project
# For more information see: https://
  pull_request:
    branches: 

jobs:

  build:
    

    - name: Set up
      uses: actions/setup-go@v4
      with:
        go-version: '1.20'

    - name: Build
      run: go build -v ./...

    - name: Test
      run: go test -v ./...
